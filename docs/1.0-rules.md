# Rules

The decisions about how the service behaves, kept apart from the design notes
that explain how they are met. Tests and commits cite the ids.

**This list is incomplete on purpose.** A rule is here because someone decided
it and it is worth remembering. Nothing being written about an area means
nobody has decided, not that anything goes.

Three questions decide whether something belongs. Was it decided, or does it
follow from the game — premium squares and word validity come from FISE and
SOWPODS, and a second copy here could only disagree with the first. Could a
reader recover the intent from the code, or only the behaviour. And would a
disagreement with the code be a bug rather than a preference.

Unlike the 4.x documents there is no freshness stamp. A reference document
that has gone stale is wrong; a rule the code disagrees with means the code is
wrong, or the rule changed and nobody said so.

## Accounts

**ACC-1** A signed-in session lasts at most 10 days, and ends after 48 hours
unused. Signing in again starts a new one.

**ACC-2** Staying logged in is a choice about what the device remembers. It
does not extend ACC-1.

**ACC-3** A player's rating history belongs to the player, not to the games
that produced it. It survives those games and goes when the account does.

## Deleting an account

This is the administrator's tool for clearing out old data, and it removes the
record outright. Closing your own account and shutting out a problematic one
are different problems: both would mark the account rather than remove it, and
neither has been analysed yet. Nothing here applies to them.

**DEL-1** An account can only be deleted through the admin CLI, run on the
machine hosting the service.

**DEL-2** An account cannot be deleted while anything still refers to it: a
game it created, a seat it holds, or a game that has invited it — whatever
state that game is in, and whether or not the seat has been given up.

**DEL-3** Declining does not release the account. The seat the invitation was
for keeps the name on the roster, so deleting the account would leave the game
offering a seat to somebody who no longer exists — which is the orphaning
DEL-2 exists to prevent. The game going is what clears it.

**DEL-4** DEL-2 is only reasonable because references expire on their own.
Every game goes eventually and takes its invitations with it, so waiting is
always an option and no account can be held open forever. RET-3 is what makes
that true of unanswered invitations, which live only on unstarted games.

**DEL-5** Removing a game from your own list hides it from you. The game
still exists and still refers to you, so DEL-2 still refuses — an account with
an empty games list is not necessarily deletable.

**DEL-6** A refusal names the games responsible.

**DEL-7** Deleting an account removes everything personal to it — sessions,
invitations sent and received, current rating, rating history — and nothing
belonging to anyone else.

**DEL-8** Deleting an account that never existed is reported differently from
refusing to delete one that does.

**DEL-9** The CLI takes a display name or an account id, and both reach the
same account.

**DEL-10** Rating history outlives the game it came from, and does not outlive
the player. A rating is a fact about a player at a moment; the game is what
caused it, not what owns it. So removing a finished game leaves its rating
history standing, while DEL-7 takes that history with the account.

## Games

**GAME-1** Aborting a game gives up every seat at once, whatever state the
game had reached.

**GAME-2** A game ends once at most one seat is still playing.

**GAME-3** A game has at least two seats. GAME-2 reads one remaining seat as
everybody else having left, which is true of a single-seat game before anybody
has done anything — so it would end on its first turn and declare that seat
the winner. Checked when a game is created and again when it starts, because
seats can be removed in between.

**GAME-4** A player may hold more than one seat in the same game, and so may
a bot. Everything that belongs to a seat — the tiles it holds, the turns that
are its own, the messages addressed to it — belongs to the seat rather than to
the player, so holding two is holding two of each.

## Ratings

**RATE-1** A game an administrator force-ends changes nobody's rating.

**RATE-2** A player who resigns is ranked and rated for the game, below
everyone who finished it. Resigning is a way of ending your game, and it
counts as one.

**RATE-3** A seat that is force-resigned or times out is left out of the
result entirely, as though it had never been in the game: not ranked, not
rated, and no part of anyone else's comparison. Neither is something the
player chose.

A losing player can exploit that by letting the clock run instead of
resigning, and pay nothing for the game. It stands anyway: most timeouts are
mistakes, and rating them would charge everyone who forgot a game for the few
who worked it out.

## Retention

**RET-1** A completed game — finished or aborted — is deleted seven days
after it ended.

**RET-2** Deleting a game leaves the rating history it produced, per ACC-3.

**RET-3** A game that never started runs a thirty-day countdown from its last
activity. Seven days in, its creator is
asked what to do with it, and the question stands until they answer or the
countdown takes the game, so it may be answered on day eight or on day
twenty-nine.

- **delete** — the game goes immediately
- **keep** — counts as activity, so the thirty days start again
- **no answer** — the countdown carries on

A creator answering keep every twenty-nine days holds a game indefinitely,
which is the point: the rule clears games nobody is waiting on, not games
somebody still wants. Not built yet, and DEL-4 depends on it.

**RET-4** A game is removed no sooner than RET-1 allows, but not necessarily
promptly. Removal happens while the service is being used, so a quiet week
delays it.

## Move time limits

**TIME-1** Every game has a move time limit. It cannot be turned off.

**TIME-2** The limit defaults to 72 hours and can be set between one hour and
forty days. So a game always ends eventually, which is what makes DEL-2
tolerable — an account blocked by a game is not blocked indefinitely.

**TIME-3** A seat that exceeds the limit is retired, and the tiles it held go
back into the bag.

**TIME-4** The player on turn is emailed once when a third of their time
remains, for limits longer than a day.

## Times and durations

**CLOCK-1** A datetime is stored and manipulated as a Unix epoch — seconds
since 1970-01-01 UTC, in an `i64`. Signed, so dates before 1970 are
representable rather than a trap for whoever first needs one.

**CLOCK-2** A duration is not a datetime. It carries no timezone and no epoch,
and is a count of a named unit. New durations are integer nanoseconds in a
`u64`, which holds about 584 years — the unit is chosen for the small end
rather than the large one, because we are far more likely to want
sub-microsecond resolution than a century of range.

**This rule postdates most of the code.** What exists, verified 2026-08-18 —
nothing anywhere uses nanoseconds:

| | unit | type | where |
| --- | --- | --- | --- |
| `move_time_limit_seconds` | seconds | `u64` | wire, database, three DTOs |
| `elapsed_us` | microseconds | `u64` | wire, `snapshot_json` |
| `*_MS` polling and retry intervals | milliseconds | `u64` | `crates/ui/src/app.rs` |
| `engine_timing_bench` | milliseconds | **`f64`** | the benchmark and its CSV |

`elapsed_us` was reasoned rather than accidental — "microseconds because bot
moves are routinely sub-millisecond" — and none of these is being churned for
consistency alone. CLOCK-2 governs what is written next, and says which way to
jump when a duration has no precedent to follow.

**The table is a list of debts, not a description of the world.** A row goes
when its unit is brought into line, so it shrinks rather than drifts. A duration
that is neither in the table nor in nanoseconds is a rule violation, not an
omission from the table.

**CLOCK-3** Administrative output is UTC — logs, the admin CLI, and any
discussion or record of an incident. Local time on a deployment VM is an
accident of provisioning, and an incident timeline is reconstructed later from
logs, CI and status pages, every one of which is UTC. Converting is where an
hour goes missing.

**CLOCK-4** A time shown to a player is local, converted at the last moment.
Nothing before the display holds a local time.

There are none yet: the client shows only *relative* times, which need no
timezone at all. So this rule governs the first absolute time we ever display.

**CLOCK-5** A displayed duration shows at most two units, always adjacent, and
the second only where a reader would act on it. `1h 5m`, never `1h 5m 30s`; `3h
ago`, because nobody acts on the difference between that and `3h 40m ago`.

**CLOCK-6** A rounded duration claims neither precision nor time it does not
have. Truncation is the default. Where truncating would state something false
that a reader would act on, say what is known instead of inventing a number:
under a minute remaining reads `<1m left`, not `0m left`, which says the turn is
lost, and not `1m left`, which promises fifty-nine seconds that may not exist.

We may switch the last minute to seconds (`45s left`), which is more use to a
player racing the clock. The countdown is redrawn only when the page is, so
seconds need a one-second tick or the client and server agreeing the moment;
that is open in #271.

## Asking too often

These protect the service from load. Stopping somebody who is behaving badly
is a different problem, dealt with by stopping their account, and nothing here
attempts it: registration is open, so a determined caller can always spread
across addresses and accounts.

**LIMIT-1** The service limits how often a caller may ask, separately from how
much work it will do at once.

**LIMIT-2** A caller over its allowance is refused, not queued.

**LIMIT-3** An allowance belongs to its caller. Being refused never depends on
somebody else's traffic, except at the global floor.

**LIMIT-4** `/health` is never limited. It has to answer while everything else
is refusing, or a busy service reports an outage that is not happening.

**LIMIT-5** Exceeding an allowance slows a caller. It does not lock them out
for a fixed window.

**LIMIT-6** Rate limits are set so that ordinary use never meets them, in the
knowledge that this leaves them useless against a determined attacker. That is
the right way round.

**LIMIT-7** Every limit is tunable per host without a rebuild.
