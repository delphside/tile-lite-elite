# Tools

Every script/binary this project provides, in one place, each linking to
the lifecycle doc that explains it in full context. For what these tools
touch (env vars, volumes, ports), see [Reference](4.1-configuration.md).

**`crates/*/examples/` is not registered**, and that is deliberate rather than
a backlog. An example demonstrates, measures or reproduces; it is not something
you are expected to find here and run. Three of them write a results CSV that is
committed, which is fine — what an example must never do is produce something
compiled into the binary. That is the line, and it is the same line
`.githooks/pre-commit` draws when it excludes `examples/` from the rule that an
image change takes a branch: anything generating a file the binary
`include_str!`s is a crate, not an example (see `crates/wordlist-tools`, #306).

An example gets a row here only when a document sends you to it, as the two
benchmarks do. That is a choice, not an obligation.

| Tool | Purpose | Explained in |
|---|---|---|
| `scripts/setup-dev-environment.sh` | Bootstrap a fresh dev machine (Rust toolchain, wasm target, `dioxus-cli`/`wasm-bindgen-cli`, `sccache`, Docker) | [Setup](3.1-setup.md#development-environment-setup) |
| `scripts/services.sh` | Start/stop/restart/status/logs for the local backend + web dev server | [Development](3.2-development.md#quick-start-scripts) |
| `scripts/desktop.sh` | Launch a desktop client against the local backend | [Development](3.2-development.md#quick-start-scripts) |
| `scripts/admin.sh` | Operator CLI (`tile-lite-elite-admin`) wrapper for a **local** dev server — list/delete users, reset passwords, list/delete/force-end games | [Production Environment & Operations](3.4-production-environment.md#admin-cli) |
| `tile-lite-elite-admin` (binary) | The same operator CLI, run directly — locally via `admin.sh`/`cargo run -p admin-cli`, or against a container via `docker compose exec` | [Production Environment & Operations](3.4-production-environment.md#admin-cli) |
| `scripts/deploy-preview.sh` | Build and run the preview stack locally: `up`/`down`/`reset`/`at <ref>`/`at prod`/`verify`. Reports failing document checks at the end — a check, not a gate | [Testing, CI & Release](3.3-testing-ci-and-release.md#26-preview-environment) |
| `docs/diagrams/` | Mermaid sources for the pre-rendered diagrams, and how to re-render them | [`docs/diagrams/README.md`](diagrams/README.md) |
| `.githooks/pre-commit` | Refuses a commit whose documentation checks fail, when markdown is staged. Runs `check-docs.sh` — the gate, where running it and gating on it stopped being different things. Also refuses an image change on `main`, and the mirror of it: the process's own documents (`CLAUDE.md`, `docs/1.6`, `3.6`, `3.7`) on a project branch, which `commit-msg` cannot catch because a process edit carries no `Refs` | [3.6](3.6-change-lifecycle.md) |
| `.githooks/commit-msg` | Refuses a subject with no version stamp, or one naming an issue that is not the branch's. Adds the co-author trailer | [3.6](3.6-change-lifecycle.md) |
| `.claude/skills/triage/` | The order to triage in — the minimum fields, then scope, then the five outcomes — and what carries a milestone, which is the part most often got wrong. Joint with the owner, except in Delivery Tooling and Process Definition and for type `tooling` or `documentation` in any workstream, where Claude triages alone — D54 | [The Change Lifecycle](3.6-change-lifecycle.md) |
| `.claude/skills/change-a-document/` | The procedure for changing anything in `docs/`: name the subject, find its section in the map, **read that section before writing**, and — if there is no such section — say whether the map, the structure or the documents are at fault rather than writing it where you are. Versioned, like `raise-project` | [1.6](1.6-document-map.md) |
| `scripts/document-map.py` | Every heading in `docs/` in one place, so a fact can be looked up rather than grepped for. `--write` regenerates `docs/1.6`, `--check` fails if it is stale. Also reports a section heading appearing twice — the defect that put two `2.13.2`s in `docs/3.3` | [1.6](1.6-document-map.md) |
| `.claude/skills/ship-a-change/` | Taking a change out: the route table that decides which steps apply, the lap in order, and the traps each of which has cost a run — preview must hold the commit being shipped, `--workers=1` against anything remote, rehearsal's rate limits relaxed **and restored**, the milestone read before deploying, `deploy.sh` run bare. Plus what is not automatic afterwards: the delivery-log row, a lettered milestone, the post-deployment answers | [3.3](3.3-testing-ci-and-release.md) · [4.9](4.9-delivery-log.md) |
| `.claude/skills/raise-project/` | The mechanics of raising a project: the seven headings, every field and option id, and the fold-and-close steps. Versioned, unlike the rest of `.claude/` | [3.6](3.6-change-lifecycle.md) |
| `scripts/roadmap-diagram.py` | **R8**: the roadmap as a Gantt without dates — workstreams as swimlanes, sequencing left to right, dependencies drawn. Bars are deliveries: work packages and standalone projects, the issues carrying a delivery milestone. A parent delivers nothing and so has no bar. Anything nothing blocks starts at the left, and every bar is the same width because width would imply a duration this chart does not have. `--parent N`, `--workstream NAME`; `--write` regenerates `docs/diagrams/roadmap.svg` **and** the block that embeds it in docs/1.5, the document that exists to be left open while working. `--project N` draws a different picture — one project's work packages with the requirements each expects to take, read from the allocation table in its own body — and `--write-issue N` puts that back into the project. Drawn as SVG, not Mermaid, which cannot do swimlanes — see [`docs/diagrams/README.md`](diagrams/README.md). Reads `scripts/board/` | [Workstreams](3.7-workstreams.md) |
| `scripts/ci-status.sh` | Did a **named** CI run pass for a commit? `--run <event>[:<branch>]` picks it and is required; `--require <job>` insists a job ran and passed; `--wait` blocks until it finishes; `deploy.sh` uses it as the release gate | [Gating on a particular run](3.3-testing-ci-and-release.md#2133-gating-on-a-particular-run) |
| `scripts/check-release-version.sh` | Refuse a patch release that carries functional change — reads the milestone's labels; `deploy.sh` runs it as a release gate | [Releases are branches](3.6-change-lifecycle.md#1-the-flow-how-a-change-reaches-production) |
| `scripts/check-commit-stamp.sh` | Check commit subjects carry `app <X.Y.Z> api <M.N>:` and that the numbers match the tree; CI runs it per push | [Version numbers: which to bump, and when](3.3-testing-ci-and-release.md#27-version-numbers-which-to-bump-and-when) |
| `scripts/deploy.sh` | `DEPLOY_GATES_ONLY=1` to ask whether a deploy would be allowed, without building anything. Otherwise: build images locally from a fresh checkout of a commit (`HEAD`, or any ref — that's also how you roll back) and ship them to the production VM | [How `deploy.sh` ships an image](3.3-testing-ci-and-release.md#213-how-deploysh-ships-an-image) · [Rolling back](3.3-testing-ci-and-release.md#211-rolling-back) · **Exits `3`** when production is live and a step after the swap failed, naming each and what it needs — see [4.8](4.8-artefacts.md#what-a-scripts-exit-status-means) for what a status means |
| `scripts/rollback.sh` | Put production back to the previous images *and* the database snapshot taken with them — `deploy.sh` calls it automatically when a deploy fails, and it is the **first thing to reach for** when production is broken | [Rolling back across a migration](3.3-testing-ci-and-release.md#2111-rolling-back-across-a-migration) |
| `e2e/` (`npm test`) | Playwright end-to-end UI tests — drive the real web client in a browser against a running dev environment | [Testing, CI & Release](3.3-testing-ci-and-release.md#1-what-to-type) · [`e2e/README.md`](../e2e/README.md) |
| `.github/workflows/advisories.yml` | Nightly `cargo audit`, run with `--deny warnings` because severity does not predict whether a finding reaches us. `.cargo/audit.toml` holds the unreachable ones with a reason each. A failing run opens or updates one untriaged issue | [Testing, CI and Release](3.3-testing-ci-and-release.md#251-the-advisory-check-and-why-it-denies-warnings) |
| `.github/dependabot.yml` | Watches the pinned GitHub Actions, weekly. Not the crates — that is the advisory check's question | [Testing, CI and Release](3.3-testing-ci-and-release.md#251-the-advisory-check-and-why-it-denies-warnings) |
| `.github/ISSUE_TEMPLATE/config.yml` | Turns **blank issues off**, so every issue arrives through a form that sets its type. A blank issue carries none, and an untyped issue is skipped by every rule keyed on type — it reads as compliant while being invisible (#361). Anything still reaching the board untyped, which the API and `gh issue create` can do, is read as a `Requirement` and reported as defaulted | [The Change Lifecycle](3.6-change-lifecycle.md#217-triage-what-happens-to-an-issue-when-it-is-raised) |
| `.github/ISSUE_TEMPLATE/requirement.yml` | The form for raising a requirement — what is wrong, its type, what carries it, and what would show it works. Sets the issue type to `Requirement`. A requirement with no workstream is the triage queue, so an unclassified requirement is queryable | [The Change Lifecycle](3.6-change-lifecycle.md#217-triage-what-happens-to-an-issue-when-it-is-raised) |
| `.github/ISSUE_TEMPLATE/project.yml` | The form for raising a project — the six headings, pre-filled. Sets the issue type to `Project` | [The Change Lifecycle](3.6-change-lifecycle.md#211-what-a-project-is) |
| `scripts/board/release.py` | What the milestone being built still owes — the tests its projects said they would run, still unrun. Moved from `verify.sh`'s `check_approach` on 2026-09-21, which asked GitHub for the milestone's projects, then each body, then each one's sub-issues to tell a parent from a package. A parent is skipped: its packages own the test approach (D51) | [3.6](3.6-change-lifecycle.md) · #383 · #289 |
| `scripts/board/overtaken.py` | A project that shipped and was still open when the next release went out. Moved from `verify.sh`'s `check_reviews` on 2026-09-21, which asked GitHub twice for what the model holds — once for the candidates, then once per project for the timeline event dating it. Dated from entering `Post-deployment`, not the phase it sits in now | [3.6](3.6-change-lifecycle.md) · #383 |
| `scripts/board/milestone.py` | Whether the milestone being built carries only work that exists — an open issue in it with no `Refs #N` commit behind it. Moved from `verify.sh`'s `check_milestone` on 2026-09-21, the last of that script's direct GitHub calls. The one part of the model that joins the board to git, so it counts what `issue-mentions.sh` counts for `deploy.sh`'s gate, merges and trailer case included: a pre-flight that passes where the gate refuses stops the release where stopping costs most. A parent is a fact, not a finding, and a package with no commits of its own stays unbuilt with its parent's count stated beside it rather than credited to it (#375) | [3.6](3.6-change-lifecycle.md) · #383 · #375 |
| `scripts/board/branches.py` | **R9.** A branch checked against the project it names — that the issue exists, is open, and is a Project (`docs/3.6` §2.12). Reported by `board-check.py`. `.githooks/commit-msg` compares the branch name against the commit trailer, two local strings, so it can say they agree and never whether what they agree on is real | [3.6](3.6-change-lifecycle.md#212-a-branch-belongs-to-a-project) · #342 |
| `scripts/board-inbox.py` | **R2.** What has been said and decided since you last looked — comments grouped by issue with the model's context beside each, and the issues opened or closed in the window, which no comment listing reveals. Takes a window in days. Retired `inbox.sh` 2026-09-21, which fetched every issue a second time to do it | [3.3](3.3-testing-ci-and-release.md#1-what-to-type) · #383 |
| `scripts/clean-test-accounts.sh` | Removes the accounts a test tool created, **from the environment it ran against** — deriving that from the URL the test was pointed at rather than defaulting. Refuses production outright, and exits non-zero when it could not reach its target rather than reporting nothing to do | [Testing, CI & Release](3.3-testing-ci-and-release.md#1-what-to-type) |
| `scripts/check-api-errors.py` | Every error the API can return, against `docs/4.3`. Reads the `ApiProblem::*` literals out of `crates/server-game/src` and names the ones the document does not carry. **A gate since 2026-09-21**; a check while the debt was 44, because a build must not refuse over writing that has not happened yet. All 70 are now documented, so a new error with no line in 4.3 is a regression. Exits 1 | [4.3](4.3-api-schema.md) · #329 |
| `scripts/comment-inventory.py` | The reasoning inside the delivery tooling, listed so a restructuring cannot lose it. Extracts every whole-line comment block citing an issue or a date, with its file, line and the function it introduces; `--write PATH` emits the inventory as markdown. **The dated blocks are the regression list** — each records a defect that actually happened, so a replacement that reintroduces one is wrong. Generated, never edited: re-run it | [3.6](3.6-change-lifecycle.md) · #383 |
| `scripts/board-check.py`, `scripts/board/` | **R4** — reads every open issue and answers each obligation its type and step carries: **met**, **missing** or **not checked**. The first piece of the board model (#383), Reports, never refuses. Whole board in ~4s, against the 46s the per-issue scripts took, because it is one query rather than one per issue | [#383 model](changes/workstreams/delivery-tooling/383-one-board-model/model.md) |
| `scripts/board-actions.py` | **R1**: what needs the owner, longest-quiet first — an unanswered decision, a pull request awaiting review, a delivery in user testing. Nothing else: a report that also says what is going well gets skimmed. Says *nothing is waiting on you* plainly rather than printing blank space, orders by how long each has waited — read from the timeline, so `12d waiting` is when the field moved and `12d quiet` is the weaker last-activity fallback — and carries the countdown to the `gh` token's expiry, which is the owner's to renew and which nothing else warns about (#309). Runs beside `actions.py`, which answers the wider question of every outstanding action. Reads `scripts/board/`. Also surfaces a recurrence — `Next … due: <date>` in an issue body — on the day it falls due, labelled like a checkbox (#291 R2) | [3.6](3.6-change-lifecycle.md) · #383 |
| `scripts/board-status.py` | **R3**: where the programme stands — what each environment runs and how far behind `main`, what a release would ship (separated from what is already live, because a Repository Change reaches users at merge), every open change with its state derived from commits rather than a field — `Closes #N` means delivered, `Refs #N` only that work touched it — and what is behind. Marks any row where the commits and the board **disagree**, and where a package's commits sit under its parent's number it says so rather than claiming it built (#375). Three sources: GitHub, git, and `/health`. Reads `scripts/board/` | [3.6](3.6-change-lifecycle.md) · #383 |
| `scripts/board-digest.py` | **D54's weekly digest**, one issue comment: what landed, what was deleted, the tooling share against the 45% limit, and anything decided the owner might have decided differently. Three parts are derived from git and the board so the digest cannot report what its author remembers rather than what happened; the fourth is a judgement about someone else's preferences, supplied with `--decided`, and an empty one says it was **not written** rather than that there was nothing. `--since` takes a tag, sha or date; `--post N` comments it on the decision | #382 · [3.7](3.7-workstreams.md) |
| `scripts/mermaid/check.mjs` | Parses every mermaid diagram in the repository — fenced blocks in markdown and `.mmd` files — using **mermaid's own grammar**, because only that agrees with what GitHub renders. Reports the file, the line the block opens on, and the parse error. Run by `check-docs.sh` step 6. Needs `mermaid` and `jsdom` (`npm install --prefix scripts/mermaid`); **without them it says so and passes**, so a machine that has them gates and one that does not is not blocked. **Local only** — CI is for releases, not for documentation and process, which run locally (#340, closed 2026-09-17) | [3.3](3.3-testing-ci-and-release.md) · #340 |
| `scripts/check-hosts.sh` | Reads kernel, Docker version and pending-reboot from rehearsal and production and reports where they differ — **#360 R3**, the two having drifted for weeks on facts that take a second to read. Also answers R2: a kernel installed and not running is not a fix. Read-only; rebooting a host is the owner's call | [3.4](3.4-production-environment.md) · #360 |
| `scripts/rehearsal-limits.sh` | Switches rehearsal between production's rate limits and ones a regression suite can survive (`show`/`regression`/`production`). Registration is 2 a minute with a burst of 3 and the suite registers an account per test, so under production's numbers **21 of 31 fail at the limiter**. A switch rather than a permanent relaxation, because `check-rate-limits.sh` exists to prove production's numbers hold there — relaxed, it would pass and mean nothing | [4.1](4.1-configuration.md) · #371 |
| `scripts/rehearsal-key.sh` | The rehearsal access key, from `REHEARSAL_ACCESS_KEY` or over ssh from the host's `.env`. Rehearsal is closed by the Caddyfile (#240) and answers 403 to everything but `/health`, so any tool that must reach it lets itself in with `Cookie: rehearsal=<key>`. **One place knows how** — `check-rate-limits.sh` learned it and the Rust example did not, which is #371, and a second copy is how that happens twice | [3.3](3.3-testing-ci-and-release.md) · #371 |
| `scripts/check-docs.sh` | The documents' own gate: markdownlint, every internal anchor link resolves across `docs/`, and change documents are in an issue or release folder. **CI runs it on every push**, so the check made by hand and the one enforced are the same code | [Testing, CI & Release](3.3-testing-ci-and-release.md#1-what-to-type) |
| `scripts/backup-to-oci.sh` | The nightly offsite backup: `VACUUM INTO` for a consistent copy with the server still running, `PRAGMA integrity_check` before trusting it, gzip, then upload through a **write-only** pre-authenticated request the VM cannot read back. Refreshes a marker object only on success, so the alarm watches for absence of success | [Production Environment & Operations](3.4-production-environment.md#backups) |
| `scripts/restore-backup.sh` | Restore from the offsite backups — **never runs on the production VM**, which holds a write-only credential by design. Takes a read pre-authenticated request made in the console for the occasion, picks the newest object, verifies it opens *and has players in it*, and loads it into a volume | [Production Environment & Operations](3.4-production-environment.md#backups) |
| `scripts/check-log-hygiene-nightly.sh` | The host's nightly wrapper: the day's journal through `check-log-hygiene.py`, exceptions appended to their own log, and a marker object refreshed only on a clean run | [Production Environment & Operations](3.4-production-environment.md#logging) |
| `scripts/check-log-hygiene.py` | Validates a stream of JSON log lines against the schema `docs/4.7` declares: no address in any field, no field the document does not declare, and no debug-only event in a production stream. **Reads; never drives** — CI feeds it the regression suite's captured output, and the nightly check on the host feeds it the day's journal | [The Change Lifecycle](3.6-change-lifecycle.md#213-the-impacted-artefacts-and-why-they-are-listed-early) |
| `scripts/check-doc-links.py` | The link half of the above, on its own — every `](…)` between documents resolves, both the file and the heading. 81 anchor links broke silently when `docs/3.3` was renumbered, and neither half errors at read time: a bad anchor lands the reader at the top of the page, a bad file at a 404 | [Testing, CI & Release](3.3-testing-ci-and-release.md#1-what-to-type) |
| `scripts/e2e-clean.sh` | Purge `e2e-*` test players and their games from the dev DB (Playwright teardown; also runnable by hand) | [`e2e/README.md`](../e2e/README.md) |
| `scripts/check-rate-limits.sh` | Does the running server refuse a caller who asks too often, and keep answering everybody else? Run against rehearsal as the release gate for a change with nothing to look at | [Testing, CI & Release](3.3-testing-ci-and-release.md#1-what-to-type) |
| `scripts/rehearsal-access.sh` | Lets one device into rehearsal, and takes every device out. `grant` prints the unlock URL as a QR code to scan; `revoke` rotates the secret; `status` says whether the key on the host is the one the container is running. Needs `qrencode` for the code, and prints the URL without it | [Production Environment](3.4-production-environment.md) |
| `scripts/bench-rehearsal.sh` | Runs the engine timing benchmark on rehearsal's hardware and records the row, printing it beside the previous run on the same host. Part of a release's technical tests. A check, not a gate — it never fails a release | [Engine Interface](2.3-engine-interface.md#reading-an-engine-timing-run) |
| `scripts/capacity-chart.py` | A bar chart per capacity metric, with an ordinary least-squares trend projected forward and the ceiling marked. Reads `docs/reports/capacity_plan/measurements.csv` and writes SVGs beside the reports; also prints the history table, a forecast summary with a reliability word and an RRMSE per metric, and a one-line verdict including when a rising row meets its ceiling. Derived, never drawn — the same argument as `roadmap-diagram.py` | [3.6](3.6-change-lifecycle.md) · #291 R1 |
| `scripts/bench-compare.py` | Compares two per-move benchmark runs, pairing on `(game, turn)` and excluding moves the hypervisor descheduled, so a VM's percentiles can be read as evidence about the code | [Engine Interface](2.3-engine-interface.md#reading-an-engine-timing-run) |
| `scripts/sync-pr-state.sh` | Puts every pull request on the board and sets `PR State` from `isDraft`, `reviewDecision` and whether a review is requested — the same ladder `board/sources.py` reads. `--check` reports the drift and changes nothing. The field is generated rather than maintained, for the reason #219 deleted the `approved` and `awaiting-review` labels: a second store for what GitHub already knows goes stale, and this one did within a day | [Change Lifecycle](3.6-change-lifecycle.md#38-pull-requests-and-what-they-are-for-here) |
| `scripts/verify.sh` | Asserts the whole world is as it should be and exits non-zero if not, where `board-status.py` only shows it. Run after every deploy, trusting the exit status rather than the output | [Testing, CI & Release](3.3-testing-ci-and-release.md#17-verify-what-is-live) |
| `scripts/merge-to-release.sh` | Merges a project's pull request into a release branch, but only when the incoming pull request's run **and** the release branch's current tip have both passed with `e2e`. A missing run is refused rather than read as success; `--check-only` asks without merging | [Testing, CI & Release](3.3-testing-ci-and-release.md#181-merge-a-project-into-a-release-branch) |
| `scripts/deploy-rehearsal.sh` | `deploy.sh` pointed at the rehearsal host: same script, same image, same steps, against a machine built to match production. Also `reset` — empty the database and restart, so a testing phase starts from nothing and any seeded production data is gone | [Testing, CI & Release](3.3-testing-ci-and-release.md#15-rehearse-the-release-and-run-the-technical-tests) |
| `scripts/rollback-rehearsal.sh` | `rollback.sh` pointed at rehearsal, so the rollback path is rehearsed on a host you can afford to break | [Testing, CI & Release](3.3-testing-ci-and-release.md#211-rolling-back) |
| `scripts/seed-rehearsal.sh` | Copies production's database to rehearsal, so a migration is rehearsed against real rows rather than an empty volume | [Testing, CI & Release](3.3-testing-ci-and-release.md#15-rehearse-the-release-and-run-the-technical-tests) |
| `scripts/mkfavicon.py` | Draws `crates/ui/assets/favicon.ico` — the tile whose letter T is also a tree, from the owner's ceramics design. Every shape is a rectangle placed on a 16-unit grid, because this machine has no image tooling; without the generator, changing the icon means redoing that placement by hand. Deterministic, so a rerun that changes nothing leaves the tree clean | [Client UI](3.7-workstreams.md) |
| `scripts/check-run-log-events.py` | **#328 R2.** Every `run_log_event` a script writes must appear in `docs/4.7`'s script table. Run by `check-docs.sh`, so an undeclared event fails CI. The server half is held true by `tests/log_schema.rs`; without this the script events drift within a month | [4.7](4.7-log-events.md) · #328 |
| `scripts/run-log.sh` | **#328.** Sourced, not run: a machine-readable record of a script run that outlives the terminal, at `${XDG_STATE_HOME:-~/.local/state}/tile-lite-elite/`. Outside the repository so two worktrees do not produce two answers; one file per run, pruned to the last 20 per script. Adds a channel and changes nothing printed. A caller with its own EXIT trap must chain it, capturing `$?` first | [4.7](4.7-log-events.md) · #328 |
| `scripts/shipping-paths.sh` | Which repository paths reach the image — `docs/4.8`'s route rule in the form a script can apply. Sourced by `.githooks/pre-commit`, which refuses such a change on `main`, and by `deploy.sh`, which marks the same commits in its build manifest. One definition, because it decides both whether a commit is refused and whether a release is described correctly. Sourced, not run | [Artefacts](4.8-artefacts.md#the-route-an-artefact-takes) |
| `scripts/rehearsal-target.sh` | Where rehearsal lives — host, key and URL, defined once. Sourced, not run | [Production Environment & Operations](3.4-production-environment.md) |
| `scripts/ssh-preflight.sh` | Proves the deploy key works before anything expensive starts. Sourced by `deploy.sh` and `rollback.sh`, not run | [Testing, CI & Release](3.3-testing-ci-and-release.md#16-ship-it) |
| `scripts/issue-mentions.sh` | How many commits reachable from a ref mention an issue. One function, sourced by `deploy.sh`'s milestone gate and `verify.sh`'s milestone check | [Testing, CI & Release](3.3-testing-ci-and-release.md#16-ship-it) |
| `scripts/read-api-version.sh` | Prints the api version as `M.N` from a Rust source file on stdin. One parser, because two drifted | [Testing, CI & Release](3.3-testing-ci-and-release.md#273-decide-whether-the-api-version-moves) |
| `cargo run --release --example rate_limit_load` | Load test: takes the limits to where they trip, on rehearsal hardware, and asserts the rules rather than the throughput. Appends to `rate_limit_results.csv`. Refuses to run against production | [Testing, CI & Release](3.3-testing-ci-and-release.md#1-what-to-type) |
| `cargo run --release --example engine_timing_bench` | How long an engine move takes, across real games, in wall clock and CPU time, with steal. Writes a summary row and a per-move file keyed by (game, turn). Appends to `engine_timing_results.csv` | [Engine Interface](2.3-engine-interface.md#reading-an-engine-timing-run) |
| `.claude/turn-check.sh` | Injects the owner's new GitHub comments, pull request body edits, **and moves of `Decision State` or `Phase`** at the start of a turn, so feedback is noticed when it is written rather than at the next session. A field is how the owner says it is Claude's turn, and it produces no comment and no body edit. Every two hours it also emits a count of what is waiting and what does not add up — a pointer, not a listing. `--reseed-bodies` runs on the `Stop` hook, recording body hashes **and field values** at the end of a turn so Claude's own changes are never reported back | [Change Lifecycle](3.6-change-lifecycle.md#38-pull-requests-and-what-they-are-for-here) |
| `.claude/inbox-hook.sh` | The same look, once, at session start, plus what `board-actions.py --claude` says is waiting and any `board-check.py` findings — the two reports that answer *what can Claude do now* | [Change Lifecycle](3.6-change-lifecycle.md#38-pull-requests-and-what-they-are-for-here) |
| `cargo run --release -p wordlist-tools --bin import-wordlist` | Normalises an upstream word list into the form the committed files hold. Reads stdin, writes stdout, redirected over `crates/rules-shared/src/<name>.txt` | [Word Lists and Dictionaries](3.5-word-lists-and-dictionaries.md) |
| `cargo run --release -p wordlist-tools --bin generate-greylist` | Proposes `greylist.txt`, the words no engine will choose, from rustrict and the curated stems. Redirected over `crates/rules-shared/src/wordlists/greylist.txt` | [Word Lists and Dictionaries](3.5-word-lists-and-dictionaries.md) |

Underlying infrastructure these scripts drive, rather than tools you invoke directly: `docker compose` (`docker-compose.yml` for production, `docker-compose.preview.yml` for preview), the shared `Dockerfile` both build from, Caddy (`Caddyfile`/`Caddyfile.preview`) for reverse-proxying and TLS, and SQLite for persistence — see [Container Topology](3.4-production-environment.md#container-topology) for the request-flow picture and [4.1 Configuration](4.1-configuration.md#infrastructure-configuration) for exactly how each is configured (journal mode, pool size, Caddy's routing/caching rules, Docker's stage/image layout).
