# Configuration

*Verified against the code at `4cfcb0e` (app 0.4.11, 2026-07-26). To see what has changed since, `git log 4cfcb0e..HEAD -- crates/`; for this document's own history, `git log --follow -- <this file>`.*

Environment and application configuration — the stuff that changes rarely
and you look up rather than read start to end. For system topology and the
deployment diagram, see [1.1 Architecture](1.1-architecture.md)'s "System
Overview". For one-time provisioning, see [Setup](3.1-setup.md). For
day-to-day commands, see the lifecycle docs (linked from
[docs/README.md](README.md)) and [Tools](3.0-tools.md). For what's
persisted, see [4.2 Database Schema](4.2-database-schema.md) and
[4.3 API Schema](4.3-api-schema.md).

## Environments

Three genuinely different places this project runs, easy to conflate since some of the same commands (`docker compose ...`) work in all three:

### Local dev machine

Where you write code, run tests, and build the images that get deployed. Has the full source tree; nothing here is what's actually serving live traffic.

**Components**: Rust toolchain + workspace crates, Docker Engine (used here only to *build* images and optionally run the stack or the preview environment for local testing — not to serve real traffic), the git clone itself (pushed to/pulled from GitHub).

**Directory structure** (repo root, `~/tile-lite-elite` in this WSL setup):

```text
tile-lite-elite/
├── crates/                  # the six workspace crates
│   ├── api/                 # shared request/response DTOs
│   ├── rules-shared/        # pure rules/scoring/move-generation
│   ├── engine-core/         # GameEngine trait + GreedyEngine
│   ├── server-game/         # Axum backend
│   ├── ui/                  # Dioxus web/desktop client
│   └── admin-cli/           # tile-lite-elite-admin operator CLI
├── old-crates/              # early prototypes, kept for design precedent only
├── docs/                    # this file and other design docs
├── scripts/                 # admin.sh, deploy.sh, deploy-preview.sh, services.sh, setup-dev-environment.sh, desktop.sh
├── data/                    # local dev SQLite file (TILE_LITE_ELITE_DATABASE_URL's default)
├── target/                  # cargo build output (gitignored)
├── .cargo/config.toml       # sccache + wasm rustflags (see 3.1-setup.md's Troubleshooting)
├── Dockerfile, docker-compose.yml, docker-compose.preview.yml, Caddyfile, Caddyfile.preview, .dockerignore
└── Cargo.toml                # workspace manifest
```

### Preview (local)

A second, disposable deployment target that runs *on the dev machine* — not a separate physical environment, but different enough in shape (its own containers, port, volume) to treat as a peer of the other two rather than a footnote. Exists purely to test a deployment (especially a schema migration) before it reaches production. See [Testing, CI & Release](3.3-testing-ci-and-release.md#26-preview-environment) for the full workflow.

**Components**: same two containers as production (`server`, `web`/Caddy), built from the identical `Dockerfile`, run via the separate `docker-compose.preview.yml` + `Caddyfile.preview` (plain HTTP, no domain, no TLS — see [Setup](3.1-setup.md#https) for why production needs a real hostname and preview doesn't).

**Directory structure**: none — it's ephemeral, built from whichever git ref you point it at (`HEAD` by default, or a specific commit via `at <ref>`/`at prod`) and torn down with `down`/`reset`. Always a fresh checkout of that commit, never the working tree — see [Deployments build a commit, never the working tree](3.3-testing-ci-and-release.md#211-deployments-build-a-commit-never-the-working-tree). The only persistent thing is its data volume:

```text
tile-lite-elite-preview-server-1, tile-lite-elite-preview-web-1   # containers
tile-lite-elite-preview-data                                       # volume — /data in the server container
```

### Oracle Cloud VM (production)

Where the live deployment actually runs. **Does not have the source tree at all** — no git clone, no `crates/`, nothing to build. Just the compose file and whatever Docker itself stores (images, volumes) — `scripts/deploy.sh` builds everything locally and ships only the finished images plus `docker-compose.yml` over.

**Components**: Docker Engine only. Two running containers (`tile-lite-elite-server-1`, `tile-lite-elite-web-1`) and three named volumes (`tile-lite-elite-data`, `tile-lite-elite-caddy-data`, `tile-lite-elite-caddy-config`) — see [Container Topology](3.4-production-environment.md#container-topology) for what each holds.

**Directory structure** (`~/tile-lite-elite` on the VM — same path as local, different machine, don't let that imply it's the same *kind* of directory):

```text
~/tile-lite-elite/
└── docker-compose.yml       # the only file that lives here
```

That's genuinely everything on disk outside of Docker's own storage. What's *inside* the containers (via `docker compose exec <service> ls ...`):

```text
server container:
├── /usr/local/bin/
│   ├── server-game          # the release binary actually running
│   └── tile-lite-elite-admin       # copied in but not running — invoked on demand via `docker compose exec`
└── /data/                   # the tile-lite-elite-data volume
    └── tile-lite-elite.sqlite3

web container:
├── /srv/                    # the built web client (index.html, assets/, wasm/) — served as static files
├── /etc/caddy/Caddyfile     # baked into the image at build time, not a volume
├── /data/caddy/             # the caddy-data volume — obtained TLS certificates live here
└── /config/caddy/           # the caddy-config volume — Caddy's own runtime state
```

Preview's server container has the same `/usr/local/bin`/`/data` shape; its web container has no `/data/caddy`/`/config/caddy` (no TLS to persist).

## Environment Variables

| Environment variable | Default | Description |
|---|---|---|
| `TILE_LITE_ELITE_BIND` | `127.0.0.1:3000` | Server listen address |
| `TILE_LITE_ELITE_DATABASE_URL` | `sqlite://data/tile-lite-elite.sqlite3` | SQLite database path |
| `TILE_LITE_ELITE_DB_MAX_CONNECTIONS` | `5` | The connection pool's size. Production has no reason to change this; it exists so #399's rehearsal drill can exhaust the pool on purpose by setting it to `1`, rather than holding five concurrent slow connections to force the same failure |
| `TILE_LITE_ELITE_API_BASE_URL` | `http://127.0.0.1:3000` | Backend URL used by clients. Set at *build* time (`option_env!`), not runtime. An explicit empty string means "same origin as the page" — see [Container Topology](3.4-production-environment.md#container-topology) |
| *(no variable)* | `8080` | Web dev server port. **Not configurable by environment** — `scripts/services.sh` passes `dx serve --port 8080` literally, and the port is hard-coded in its health check and status output too. Change it in that script, in all four places, or pass `--port` to `dx serve` by hand |
| `TILE_LITE_ELITE_PUBLIC_BASE_URL` | `http://127.0.0.1:8080` | Where the web client is served from — used only to build the link inside emails (password reset, welcome, invitation) |
| `RESEND_API_KEY` | unset | [Resend](https://resend.com) API key for sending email. Unset means no provider is configured — every email-triggering flow (register, invite, forgot-password) still works, it just logs the message instead of sending it (see `crates/server-game/src/email.rs`). Email wording itself lives in `crates/server-game/emails/*.txt`, not Rust code. Set via a `.env` file next to `docker-compose.yml`, **not** committed to git — see [Container Topology](3.4-production-environment.md#container-topology) |
| `RESEND_FROM_ADDRESS` | `Tile Lite Elite <noreply@mail.tileliteelite.com>` | The `From:` address emails are sent as. Deliberately a subdomain (`mail.tileliteelite.com`), not the root domain — keeps Resend's required SPF/DKIM records completely separate from the root domain's own mailbox (GoDaddy Email) records, so nothing here can affect real mail deliverability |
| `RUST_LOG` | `server_game=info,tower_http=info,warn` | Log verbosity for `server-game`. See [Logging](3.4-production-environment.md#logging) |
| `TILE_LITE_ELITE_BUILD_ID` | unset | Optional build identifier baked in at *build* time (`option_env!`), appended as SemVer build metadata to the app version (e.g. `0.4.6+a1c9f02`). Unset (the default when building by hand) shows only `Major.Minor.Patch`; `scripts/deploy.sh`/`deploy-preview.sh` set it automatically. See [Versioning](#versioning) |
| `PROD_URL` | `https://tileliteelite.com` | Where to read production's live version. Used by `scripts/deploy-preview.sh`'s `at prod`/`verify` modes, and by `scripts/deploy.sh` for the schema-version gate. See [Preview Environment](3.3-testing-ci-and-release.md#26-preview-environment) |
| `SNAPSHOT_KEEP` | `5` | How many pre-deploy database snapshots `scripts/deploy.sh` leaves on the VM. See [Rolling back across a migration](3.3-testing-ci-and-release.md#2111-rolling-back-across-a-migration) |
| `STAGING_URL` | `http://localhost:8081` | Used only by `scripts/deploy.sh`, to confirm local preview is running the exact commit about to be shipped before proceeding. See [How `deploy.sh` ships an image](3.3-testing-ci-and-release.md#213-how-deploysh-ships-an-image) |
| `DEPLOY_HOST` | `129.151.69.246` | The production VM. Used by `deploy.sh` and `rollback.sh` |
| `DEPLOY_USER` | `ubuntu` | SSH user on that VM |
| `DEPLOY_SSH_KEY` | `~/.ssh/oracle_tile_lite_elite` | Private key for it — see [3.1 Setup](3.1-setup.md) |
| `DEPLOY_REMOTE_DIR` | `tile-lite-elite` | Directory on the VM holding `docker-compose.yml`, the `snapshots/` folder, and `.env` |
| `DEPLOY_GATES_ONLY` | unset | `1` runs every gate and stops before anything is built, changed or transferred. Answers "would this deploy be allowed?" without finding out the expensive way, and is what `scripts/tests/deploy.test.sh` drives |
| `DEPLOY_EMERGENCY` | unset | A sentence saying why production is being deployed outside the normal path. Skips the preview and rehearsal gates — the two that cost an image build — and nothing else; CI still has to pass, e2e included. Raises a retrospective issue and leaves the milestone open. See [3.3, Emergency changes](3.3-testing-ci-and-release.md#212-emergency-changes), which says to try `rollback.sh` first |
| `DEPLOY_SKIP_CI` | unset | `1` bypasses the CI gate. For GitHub itself being unreachable — not a way past a failing test, and not part of `DEPLOY_EMERGENCY` |
| `DEPLOY_SKIP_BUMP` | unset | `1` leaves the dev version alone after a deploy. Rarely needed: the bump is already skipped automatically when the deployed commit isn't the branch tip |

### Environment-specific values

The table above gives each variable's bare/local-dev default; here's what actually differs across the three environments day to day (blank = not applicable/not set there):

| Variable | Local dev | Preview | Production |
|---|---|---|---|
| `TILE_LITE_ELITE_BIND` | `127.0.0.1:3000` | `0.0.0.0:3000` (in-container; only reachable via `web`) | `0.0.0.0:3000` (in-container; only reachable via `web`) |
| `TILE_LITE_ELITE_DATABASE_URL` | `sqlite://data/tile-lite-elite.sqlite3` (bind-mounted file) | `sqlite:///data/tile-lite-elite.sqlite3` (`tile-lite-elite-preview-data` volume) | `sqlite:///data/tile-lite-elite.sqlite3` (`tile-lite-elite-data` volume) |
| `TILE_LITE_ELITE_PUBLIC_BASE_URL` | `http://127.0.0.1:8080` | `http://localhost:8081` | `https://tileliteelite.com` |
| `TILE_LITE_ELITE_API_BASE_URL` (build-time) | `http://127.0.0.1:3000` | `""` (same-origin) | `""` (same-origin) |
| Web client port | `8080` (`dx serve`) | `8081` (Caddy, plain HTTP) | `80`/`443` (Caddy, HTTPS) |
| `RESEND_API_KEY` | unset (emails log instead of send) | unset unless deliberately set | set via `.env` on the VM |

The dev/preview/production split for `TILE_LITE_ELITE_BIND` and same-origin `TILE_LITE_ELITE_API_BASE_URL` is what lets one compiled wasm bundle work unmodified in both preview and production — see [Container Topology](3.4-production-environment.md#container-topology)'s "Why one image serves both" note.

## Rate Limits

How often a caller may ask, as distinct from how much heavy work runs at once
(`TILE_LITE_ELITE_HASH_CONCURRENCY`, `TILE_LITE_ELITE_ENGINE_CONCURRENCY`).
Set on the server; each is requests per minute, with a burst on top so an
ordinary caller never meets it.

**How the two numbers work.** Each limit carries an *allowance*: the number of
requests that would be accepted right now, before a refusal. `BURST` is the
allowance's maximum — the most that can ever be accepted at once. The allowance
starts at that maximum. `PER_MIN` is the rate it refreshes at: one unit every
`60 / PER_MIN` seconds, and that interval is the **period**. Accepted requests
spend the allowance, refresh restores it, and it never rises above the maximum.

**Refresh is spread, not granted at the top of a minute.** One unit returns one
period after the request that spent it, whenever that was; there is no window
and no reset instant. LIMIT-5 requires this — a caller over the limit is slowed
rather than shut out until a boundary passes.

A caller asking faster than the refresh rate drains the allowance to zero and
is then served at exactly the refresh rate, everything beyond it refused. The
traffic itself does not slow down; the excess is turned away. A caller asking
more slowly never meets any of this, which is what the burst is for.

Registration at `2/min, burst 3` is three registrations back to back from idle,
then one every thirty seconds — its period. `Retry-After` is built from the wait
until the next unit of allowance arrives, rounded up and given a small random
margin; [4.3](4.3-api-schema.md) has the derivation and why it is not a fixed
property of the limit.

| variable | default | applies to |
| --- | --- | --- |
| `TILE_LITE_ELITE_LIMIT_REGISTER_PER_MIN` | 2 | `/auth/register`, keyed by client address |
| `TILE_LITE_ELITE_LIMIT_REGISTER_BURST` | 3 | |
| `TILE_LITE_ELITE_LIMIT_AUTH_PER_MIN` | 10 | login and the password-reset pair, keyed by client address |
| `TILE_LITE_ELITE_LIMIT_AUTH_BURST` | 10 | |
| `TILE_LITE_ELITE_LIMIT_SESSION_PER_MIN` | 240 | everything a signed-in caller does, keyed by session |
| `TILE_LITE_ELITE_LIMIT_SESSION_BURST` | 60 | |
| `TILE_LITE_ELITE_LIMIT_GLOBAL_PER_MIN` | 1200 | the whole service, unkeyed |
| `TILE_LITE_ELITE_LIMIT_GLOBAL_BURST` | 200 | |

**Preview and dev raise all of them** — `docker-compose.preview.yml` and
`scripts/services.sh` respectively. The whole
Playwright suite comes from one address and every test registers a player, so
three tests exhaust a burst of 3 and the rest are told to wait — longer than
the suite waits for anything.

**Rehearsal keeps the defaults**, and is the only environment besides
production that does. It is where `check-rate-limits.sh` measures them and
where load testing runs, so production-shaped numbers belong there. The other
two exist to let you look at a change, and limits sized for a 2 vCPU VM only
get in the way of that. Setting any of these in the environment
still overrides preview's numbers, which is how limiting is exercised there on
purpose.

**What is limited, and what deliberately is not.** The same four tiers as a
grid of *what is counted* against *who it is counted for*. A blank is a
combination not limited at all:

| | per IP | per session | per account | global |
| --- | --- | --- | --- | --- |
| **registrations** | 2/min, burst 3 | | | |
| **logins and password resets** | 10/min, burst 10 | | | |
| **authenticated requests** | | 240/min, burst 60 | | |
| **any request** | | | | 1200/min, burst 200 |

**The account column is empty by design.** Authenticated limits key on the
session token, not the account: resolving a token to an account needs the
database, and the key is chosen before any handler runs. Session coincides with
account for a caller reusing one — a bot harness — and diverges for a person
with three devices, who gets three allowances.

**Authenticated traffic has no per-IP limit**, so several sessions from one
address get several allowances between them, bounded only by the global floor.
Collecting the sessions is itself limited, but one lasts up to ten days
(`ACC-1`).

**The client address is Caddy's, not the caller's.** With no `trusted_proxies`
configured, Caddy replaces `X-Forwarded-For` with the address the connection
came from, so a caller cannot forge one, spread across imaginary addresses, or
lock out somebody else by claiming to be them. **This holds only while nothing
can reach the server except through Caddy** — which `docker-compose.yml`
enforces by giving the `server` service no `ports:`. Adding one would let a
caller supply the header and be believed.

A consequence for anybody testing: every request from one machine is one
caller, so per-caller separation cannot be observed from outside. It is covered
in-process by `one_callers_allowance_is_not_anothers`.

The burst is what decides whether an ordinary caller ever meets a limit: the
rate refills one permit at a time, and the burst is how many can be spent at
once before that matters. Tune it alongside the rate rather than instead of it.

### Tuning a host

The limits are read when the router is built, so a change needs a container
restart and not a new image:

```bash
# on the host, beside the secrets Compose already reads
echo 'TILE_LITE_ELITE_LIMIT_REGISTER_PER_MIN=5' >> .env
docker compose up -d server
./scripts/check-rate-limits.sh https://<host>
```

`.env` is created by hand and `deploy.sh` never touches it, so a host keeps
its tuned numbers across every redeploy. An empty value means the default,
which is what an unset one in Compose becomes.

**A regression suite cannot run under these numbers**, and that is not a fault
in either. Registration is 2 a minute with a burst of 3; the e2e suite registers
an account per test, so the fourth test onwards is refused. Measured 2026-09-19
on the same code and host, the limits being the only difference:

| | failed | passed |
| --- | --- | --- |
| relaxed limits | 0 | 31 |
| production's limits | 21 | 10 |

A refused registration looks exactly like broken sign-up, which is the same
confusion the access gate one layer out produced in #286.

`scripts/rehearsal-limits.sh regression` relaxes a host for a run and
`… production` restores it. **A switch rather than leaving rehearsal relaxed**:
`check-rate-limits.sh` exists to prove production's numbers hold there, and
against 6000 registrations a minute it would pass and mean nothing. Restore it
when the run is done — `… show` says which is in force.

`/health` and the loopback-only `/admin/*` routes carry no limit at all.

The client address comes from `X-Forwarded-For`, trusted **only because the
server publishes no ports and Caddy is the sole ingress**. That precondition
sits beside the code depending on it; if anything else can reach the server,
the header is attacker-controlled.

The defaults are a first guess for a 2 vCPU, 954 MB box.
`./scripts/check-rate-limits.sh` asserts the shape holds against a running
server, and is meant for the rehearsal host — numbers checked on a development
machine say nothing about the one they protect.

## Infrastructure Configuration

The actual runtime settings of the two pieces of infrastructure this project depends on, verified against the live config/code rather than assumed — worth checking here before assuming a default.

### SQLite

`persistence::connect` (`crates/server-game/src/persistence.rs`) does exactly this and nothing else:

```rust
SqliteConnectOptions::from_str(database_url)?.create_if_missing(true)
```

`persistence::connect` sets `journal_mode` explicitly (`0.3.0`, see below); synchronous and foreign-key pragmas are left at SQLite's own defaults. Verified directly against a live database (`sqlite3 <file> "PRAGMA journal_mode; PRAGMA synchronous; PRAGMA foreign_keys;"`):

| Pragma | Actual value | Means |
|---|---|---|
| `journal_mode` | `wal` (since `0.3.0`; was `delete` before) | Readers no longer block behind a writer's transaction — matters once more than a handful of players are concurrently active. Sets `-wal`/`-shm` sidecar files alongside the main database file (`3.2-development.md`'s "Resetting the Database" step already correctly hedges on this — "may not exist depending on journal mode" — now they do). Volume backups (`3.4-production-environment.md#backups`) already tar the whole directory, so this needed no backup-procedure change. |
| `synchronous` | `2` (`FULL`) | Unchanged by the `journal_mode` switch (verified — sqlx doesn't adjust it automatically the way the `sqlite3` CLI's shorthand modes sometimes imply). Every commit still `fsync`s before returning. `NORMAL` (`1`) is commonly paired with WAL for a latency win, since WAL's own durability model needs less aggressive fsyncing — not changed here since it wasn't part of this release's scope, but worth a deliberate look if write latency ever becomes a real concern. |
| `foreign_keys` | `0` (off) | **The `foreign key to X.id` relationships documented throughout [4.2 Database Schema](4.2-database-schema.md) are not actually enforced by SQLite** — they're honored by application logic only (verified: `delete_player`/`delete_game` in `persistence.rs` already delete child rows before parent rows inside a transaction, in the right order). Turning this pragma on wouldn't currently change that, though: `crates/server-game/migrations/0001_baseline.sql` has no `foreign key ... references ...` clauses at all — the schema doc's "foreign key to X" language describes the *logical* relationship, not a declared SQL constraint. Real enforcement needs both the pragma **and** adding actual `references` clauses, and SQLite can't `alter table` a constraint onto an existing table — it means recreating each table. That's schema-migration-sized work, not a config change; not undertaken as part of this pass. |

Connection pool: `SqlitePoolOptions::new().max_connections(5)` — one shared pool per process, same in every environment (dev/preview/production all run a single `server-game` process each; there's no multi-process fan-out that would make 5 a bottleneck).

### Caddy

Version pinned via `caddy:2-alpine` (`Dockerfile`'s `runtime-web` stage) — floats on Caddy's `2.x` line, not pinned to an exact patch version.

- **Compression**: `encode zstd gzip` — zstd preferred, gzip fallback, for every response Caddy serves (static assets and proxied API alike).
- **Routing**: an explicit `route { }` block, not Caddy's default directive ordering — needed because Caddy's automatic ordering runs `try_files`/`file_server` *before* `reverse_proxy`, which would rewrite every API path to `/index.html` before the `@api` matcher ever saw it. The `@api` matcher lists exact paths/prefixes (`/health /engines /auth/* /games /games/* /players/* /invitations/* /dictionaries/*`) — a new route added to the server's API needs a matching entry here too, or it'll silently fall through to the SPA fallback instead of reaching `server-game`. (`Caddyfile.preview` duplicates this same matcher list — see [3.3 Testing, CI & Release](3.3-testing-ci-and-release.md#26-preview-environment) — so a new route needs updating in both files.)
- **Caching**: `/assets/*` gets `Cache-Control: public, max-age=31536000, immutable` (content-hashed filenames, safe to cache forever); `index.html` itself is deliberately left with no caching directive, since it's what points at the current asset hashes.
- **TLS**: fully automatic via Let's Encrypt — see [3.1 Setup](3.1-setup.md#https) for the sslip.io/real-domain mechanics. No manual certificate handling anywhere.
- **Admin API**: Caddy's own runtime admin endpoint (`localhost:2019`, visible in its startup log) is **not** exposed via `docker-compose.yml`'s `ports:` — reachable only from inside the container, not the host or internet.
- **No explicit resource limits**: neither `docker-compose.yml` nor `docker-compose.preview.yml` sets `mem_limit`/`cpus`/`deploy.resources` on any service — both containers share whatever the VM (1GB RAM + swap, see [3.1 Setup](3.1-setup.md#oracle-cloud-vm-setup)) or dev machine has available, uncapped.

### Docker

Multi-stage `Dockerfile`, one shared `builder` stage (`rust:1-bookworm`) feeding two final images — see [3.4 Production Environment & Operations](3.4-production-environment.md#container-topology) for the request-flow/volume picture; this is the build side.

| Stage | Base image | Produces |
|---|---|---|
| `builder` | `rust:1-bookworm` | Compiles `server-game`/`admin-cli` (release) and the web client (`dx build --release`) — not shipped itself, just feeds the two below |
| `runtime-server` | `debian:bookworm-slim` + `ca-certificates` | `server-game` + `tile-lite-elite-admin` binaries only |
| `runtime-web` | `caddy:2-alpine` | The built wasm/static bundle + `Caddyfile` |

Built images (local, `docker compose build`, `TILE_LITE_ELITE_BUILD_ID` set): `tile-lite-elite-server` ≈199MB, `tile-lite-elite-web` ≈92MB — small enough that `scripts/deploy.sh`'s `docker save | gzip` / `scp` round trip (see [How `deploy.sh` ships an image](3.3-testing-ci-and-release.md#213-how-deploysh-ships-an-image)) stays fast even over a modest connection. `.dockerignore` excludes `target`, `.git`, `data`, `.dioxus`, log/pid files, and `node_modules` from the build context, so none of those slow down the context upload to the Docker daemon or accidentally end up in an image layer.

Both runtime images define a `HEALTHCHECK` (since `0.3.0`): `runtime-server` curls its own `/health` directly (`curl` added to that stage solely for this — the only new package either image needed); `runtime-web` hits Caddy's admin API (`127.0.0.1:2019/config/`, alpine's built-in busybox `wget`) rather than the public `/health` path. That wasn't the first design — hitting `/health` through Caddy's own reverse-proxy path would check more (that Caddy can actually reach the server, not just that Caddy is up), but it **failed on real production once actually deployed**: Caddy's global HTTP→HTTPS auto-redirect fires on `:80` for any `Host`, including a bare loopback probe matching none of the configured hostnames, and the `:443` TLS handshake then fails (no certificate for that SNI). Preview's `Caddyfile.preview` has no TLS/redirect at all, so this only surfaced in production — worth remembering next time something "verified in preview" needs re-checking against whatever specifically differs in production, not treated as fully proven. (Also hit along the way: busybox `wget` resolves `localhost` to the IPv6 loopback first, and Caddy's admin listener is IPv4-only — use `127.0.0.1` explicitly or get a misleading "connection refused.") Neither compose file overrides the `--interval`/`--timeout`/`--start-period` set in the `Dockerfile`. Visible via `docker compose ps` (a `healthy`/`unhealthy`/`starting` column) or `docker inspect --format='{{.State.Health.Status}}' <container>`.

## Versioning

Three numbers travel with a build, and they answer different questions. The
first two are chosen by a person; the third is derived and cannot be wrong.

| | **API contract version** | **App version** | **Build id** |
| --- | --- | --- | --- |
| answers | can we talk to each other? | which release is this? | which commit is this? |
| form | `Major.Minor` — `2.10` | `Major.Minor.Patch` — `0.6.0` | a short commit id — `b717733` |
| lives in | `api::API_VERSION` | root `Cargo.toml`, workspace-inherited | `TILE_LITE_ELITE_BUILD_ID`, set at image build |
| changes when | a route or DTO changes, or a new *value* becomes acceptable | every release | every commit that gets built |
| chosen by | whoever makes the wire change, as they make it | the lane the release is in | nobody — it is the commit |
| published as | `/health`'s `api_version` | `/health`'s `app_version`, before the `+` | `x-build-id` on every server response; `/version.txt` from the web container; after the `+` in `app_version` |
| checked by | the client, on connect | nothing automatic | the client, on every response |
| a mismatch means | major: stop, the contract is broken. minor: carry on, say so | nothing on its own | see the three-way comparison below |

Their lifecycles are independent, which is the point. A release always moves
the app version and the build id; it moves the API version only if the wire
contract actually changed. A client one build behind can talk to the server
perfectly well — the normal case during a deploy — while a client one *major*
API version behind cannot talk to it at all.

**One id, published by both containers.** The build id is compiled into the
server *and* into the wasm, and written into the web image as `/version.txt`.
So a running tab knows three of them, and every comparison it needs is plain
equality:

| it knows | from |
| --- | --- |
| its own build | compiled in |
| the **web container's** build | `GET /version.txt` |
| the **server's** build | `x-build-id`, on every response |

| observation | what it means |
| --- | --- |
| web ≠ server | a deploy is in flight — wait, do not reload |
| client = web | up to date — the container is still serving this build |
| otherwise | new code is really being served — reload |

The middle row is the one that needs the web container. The server can only
report its own build; it cannot see what Caddy is handing out, and during a
deploy they are briefly different commits. A tab that reloaded on the header
alone would fetch the old bundle, see the header again, and loop.

**`/version.txt` used to hold a hash of the built files**, which was the same
information the long way round: it only ever changed *because* the build id is
compiled into the wasm, so it was an indirection over the commit that could not
say which commit it meant. Holding the id directly is one concept fewer and
makes the deploy window detectable rather than merely survivable.

**A dev build sets no build id**, so no `version.txt` is written and no header
is sent. Every comparison above then answers "cannot tell", and the client
declines to reload — which is correct, and is the behaviour to preserve in any
change here. Not knowing must never become a reason to act.

That is not an omission to be tidied up later. **A dev build is of the working
tree; every other environment builds a checked-out commit.** Preview, rehearsal
and production all go through a fresh `git worktree` at a specific commit (see
[3.3](3.3-testing-ci-and-release.md#211-deployments-build-a-commit-never-the-working-tree)),
so the id they stamp is exactly true of what they are running. A working tree
with uncommitted changes has no commit that describes it, and stamping `HEAD`
anyway — tempting, since it would fill in the version in the top bar — would
have a build claim an identity that is not its own. Silence is the only honest
answer available, so silence is what it says.

![Where each version comes from, and the two comparisons a running tab
makes](diagrams/version-lifecycle.svg)

*Source: [`diagrams/version-lifecycle.mmd`](diagrams/version-lifecycle.mmd) —
edit and re-render together, see [the diagram
notes](diagrams/README.md).*

**A tab asks two different places two different questions**, and that is the
part worth holding on to.

Almost all its traffic is with the **server**: Caddy proxies `/health`,
`/auth/*`, `/games*`, `/players/*`, `/invitations/*` and `/dictionaries/*` to
it, and every one of those responses carries `x-build-id`. But the server can
only say what *the server* is running. It has no idea what bundle Caddy is
handing out — they are separate images in separate containers, and during a
deploy they are briefly not the same commit.

The **web container** answers exactly one question: `/version.txt`, served as a
file from its own image and deliberately `Cache-Control: no-store`. It holds
the build id of that image, so it is the only thing that can say what is
*actually being served right now*. It is the one request a running tab makes
that does not go to the server.

Which is why the reload takes both: the header says *something moved*, cheaply
and on traffic that was happening anyway; `/version.txt` says *and the new code
is really there*, which is the only claim worth reloading on.

The rest of this section is the detail behind that table. See also the doc
comments on `api::API_VERSION` and each binary's `app_version()`, and
[4.3](4.3-api-schema.md#x-build-id) for the header.

Two independent version numbers, on purpose — see the doc comments on
`api::API_VERSION` and each binary's `app_version()` (in `server-game`'s
and `tile-lite-elite-ui`'s `main.rs`) for the full rationale.

**API contract version** (`Major.Minor`, e.g. `1.1`) — lives once in the
shared `api` crate as `api::API_VERSION`, so both the server and any given
client binary embed whatever it was at *their own* build time. Bump
`major` for a breaking DTO/route change, `minor` for an additive,
non-breaking one. The desktop client checks this against the server's
`/health` response the moment it first connects
(`check_api_version`/`compare_api_version` in `crates/ui/src/app.rs`): a
major mismatch blocks further use with an "update the app" message, a
minor mismatch shows a soft, non-blocking notice. There's deliberately no
patch/build component here — those never change the wire contract, so
including them would make the check fire on every routine bugfix deploy.
The only production client today is the wasm build shipped in the same
image as the server, so it's always in step and a mismatch has never
actually been observed — that won't stay true forever (e.g. a desktop
build lagging a server redeploy), so bump this on every route/DTO change
as a habit now, not just once it starts mattering.

**"New functionality" includes a new accepted *value*, not just a new type.**
Adding an edition to the registry means `variant` accepts something an older
client has never heard of — additive, non-breaking, and a minor bump. This is
the case that has actually been missed: 0.4.13 shipped the `spicy` edition
with the API left at 2.5, so no client saw skew, and open tabs kept working
happily while being unable to offer the edition they'd never been told about.
Ask "can a client observe something it couldn't before?", not "did a struct
change?".

The web client's auto-reload **used** to key on API skew alone, which made
forgetting this bump strand every open tab on old code. It no longer does: the
server stamps its build id on every response, so a tab notices new code whether
or not the wire contract moved. That removes the punishment, not the
obligation — the API version still has to be right for the things only it can
answer: a desktop client deciding whether it can talk to this server at all, a
third-party client doing the same, and any comparison of `api_version` across
environments meaning what it says.

**Bump per real wire change, even when production skips the intermediate
versions.** The version number describes the *artifact*, not production's
current view of it — preview or a test box can be checked out at any
committed version, so each wire change earns its own increment to keep a
cross-environment `api_version` comparison meaningful. Don't collapse an
intermediate bump just because prod jumped past it. Worked example: prod
sat at `1.4` while dev moved `1.4 → 2.0` (timestamps became integers —
breaking) `→ 2.1` (added the `aborted` `GameStatus` variant — additive)
across two undeployed commits; both bumps were kept, and the next deploy
simply takes production `1.4 → 2.1` in one step.

**App/build version** (`Major.Minor.Patch[+build]`) — `Major.Minor.Patch`
is **workspace-inherited**: it lives once in the root `Cargo.toml`'s
`[workspace.package]` (`version = "…"`) and every crate takes it via
`version.workspace = true`, so a bump is a single one-line edit
(converted from per-crate versions 2026-07-21). Read the current value
from the root `Cargo.toml` rather than from here — it moves every
release, so any number quoted in this doc would be stale by design. When
`deploy.sh` moves it, and to what, is
[3.3 §2.7.2](3.3-testing-ci-and-release.md#272-decide-whether-the-app-version-moves).
An optional
`+<build>` suffix (standard SemVer build metadata) is appended when
`TILE_LITE_ELITE_BUILD_ID` is set at compile time — e.g. a git short SHA,
for telling builds apart:

```bash
# Internal/test build with a build id
TILE_LITE_ELITE_BUILD_ID=$(git rev-parse --short HEAD) cargo build -p server-game --release

# Production release — no build id set, shows "0.4.6" not "0.4.6+..."
cargo build -p server-game --release
```

`scripts/deploy.sh` and `scripts/deploy-preview.sh` both set
`TILE_LITE_ELITE_BUILD_ID` automatically, from the short SHA of the commit
they were asked to build (`HEAD` when none was given), and pass it through
`build.args` into the `Dockerfile` — every image built through either
script carries its commit without a manual step; building by hand just
omits the suffix. Since both build a fresh checkout of that commit rather
than the working tree, the suffix is a fact about the image's contents, not
a label applied to whatever happened to be on disk.

`server-game` logs its app version alongside the API version at startup,
and serves it in the `app_version` field of `GET /health`'s response body
(`api::HealthDto`); the desktop client puts its app version in the window
title. `/health` specifically is what [Preview
Environment](3.3-testing-ci-and-release.md#26-preview-environment)'s `deploy-preview.sh
at prod`/`verify` read to find out which commit is actually live, without
needing to SSH in.
